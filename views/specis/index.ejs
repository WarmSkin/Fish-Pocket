<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
  <h1><%= title %></h1>
  <% if(specis.length) {%>
    <% specis.forEach(fishData => { %>
      
    <% }) %>
  <% } %>
</main>

<%- include('../partials/footer') %>
